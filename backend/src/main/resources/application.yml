
server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/whistleblower
    username: postgres
    password: postgres

  cache:
    type: redis
    redis:
      time-to-live: 600000
    
  data:
    redis:
      host: localhost
      port: 6379
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

